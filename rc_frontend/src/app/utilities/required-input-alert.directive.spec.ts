import { RequiredInputAlertDirective } from './required-input-alert.directive';

describe('RequiredInputAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredInputAlertDirective();
    expect(directive).toBeTruthy();
  });
});
